package com.homerun.meta.ui.equip {

	/**
	 * @author Lachhh
	 */
	public class MetaEquipBandana extends MetaEquipment {
		public function MetaEquipBandana(id:int, titleId:int, descId:int, price:int, iconId:int, isPremium:Boolean, pow:int, str:int, pre:int, con:int) {
			super(id, titleId, descId, price, iconId, isPremium, pow, str, pre, con);
		}
		
	}
}
