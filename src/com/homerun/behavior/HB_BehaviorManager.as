package com.homerun.behavior {
	import com.lachhhEngine.games.Game;
	import com.lachhhEngine.games.behaviors.BehaviorFactory;
	import com.lachhhEngine.games.behaviors.BehaviorManager;

	/**
	 * @author Lachhh
	 */
	public class HB_BehaviorManager extends BehaviorManager {
		public function HB_BehaviorManager(context : Game, behaviorFactory : BehaviorFactory) {
			super(context, behaviorFactory);
		}
		
		
	}
}
